module Network.DFINITY.Security (rateLimiter) where

import Data.Conduit (ConduitM, awaitForever, yield)

import Network.DFINITY.Message

rateLimiter
  :: Monad m
  => ConduitM Message Message m ()
rateLimiter = do
  -- TODO (enzo): Implement rate limiter.
  awaitForever yield
