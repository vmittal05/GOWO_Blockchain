module Network.DFINITY.Metric (Metric(..), encode) where

import Control.DeepSeq                  (NFData(..))
import Data.Sequence                    (Seq, singleton)
import Network.Monitoring.Riemann.Event (Event, emptyEvent)

data Metric =
  -- TODO (derek): Define data constructors.
  Metric

instance NFData Metric where
  rnf = flip seq ()

encode :: Metric -> Seq Event
encode = const $ singleton emptyEvent
